framework:
    router:
        # Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
        # See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
        # default_uri is optional - commented out to avoid requiring DEFAULT_URI env var
        # If needed, uncomment and set DEFAULT_URI in your .env file or Render environment variables
        # default_uri: '%env(DEFAULT_URI)%'

when@prod:
    framework:
        router:
            strict_requirements: null
